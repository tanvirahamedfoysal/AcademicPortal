import RepositoryManager from '../../../components/dashboard/RepositoryManager';

export default function AdminRepositoryPage() {
  return <RepositoryManager canManage title="Research Repository" description="Curate shared research papers, datasets, working documents, and downloadable materials for the academic community." />;
}
