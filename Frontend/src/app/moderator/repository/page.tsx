import RepositoryManager from '../../../components/dashboard/RepositoryManager';

export default function ModeratorRepositoryPage() {
  return <RepositoryManager canManage title="Research Repository" description="Maintain the shared academic archive and keep research resources accessible to contributors and students." />;
}
