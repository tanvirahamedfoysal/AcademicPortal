import RepositoryManager from '../../../components/dashboard/RepositoryManager';

export default function StudentRepositoryPage() {
  return <RepositoryManager title="Research Repository" description="Access shared papers, datasets, reference material, and supporting research documents from one curated archive." />;
}
